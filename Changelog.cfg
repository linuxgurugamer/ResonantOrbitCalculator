KERBALCHANGELOG
{
    showChangelog = True
    modName = Resonant Orbit Calculator
    license = MIT
    author = Linuxgurugamer

    VERSION
    {
        version = 0.0.1
        CHANGE
        {
            change = Initial release
            type = add
        }
    }

    VERSION
    {
        version = 0.0.8
		
        CHANGE
        {
            change = fixes for localization and conversion.
            type = add
        }
    }

    VERSION
    {
        version = 0.0.7.1
	
        CHANGE
        {
            change = Fixed the tooltip window always being shown even when tooltips were disabled
	        change = Updated version file to use github for new version info
            type = add
        }
    }

    VERSION
    {
        version = 0.0.7 
	
        CHANGE
        {
            change = Added LOS length calculation and display
            type = add
        }
        CHANGE
        {
	        change = Added buttons to select units display
		        subchange = m, km, Mm, Gm
            type = add
        }
        CHANGE
        {
            type = fix
	        change = Fixed bug with up/down arrows not being displaed after 2nd display
	        change = Replaced all GUILayout.BeginHorizontal() and GUILayout.BeginVertical() with the "using (new GUILayout.HorizontalScope())" and "using (new GUILayout.VerticalScope())"
	        change = Small optimization by moving init of GUIStyles into ToolbarRegistration
        }
    }


    VERSION
    {
        version = 0.0.6.2
	
        CHANGE
        {
            change = Added informative text to explain how to use with KAC when KAC is available
            type = add
        }
    }

    VERSION
    {
        version = 0.0.6.1
	
        CHANGE
        {
            change = Fixed incorrect alarms being added to KAC for sat deployments
	        change = Fixed up/down arrows being shown in the increment/decrement buttons
            type = add
        }
    }

    VERSION
    {
        version = 0.0.6
	
        CHANGE
        {
            change = Added InstallChecker
	        change = Updated for KSP 1.8
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.5.2
	
        CHANGE
        {
            change = Moved images back into PluginData 
		    change = Fix planet preview generation
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.5.1
	
        CHANGE
        {
            change = Moved thumbnail generation from the main menu to the space center scene
	        change = Added check for Kopernicus already generated
            type = add
        }
    }

    VERSION
    {
        version = 0.0.5
	
        CHANGE
        {
            change = Moved images out of pluginData folder, for performance (toolbar now uses the unBlur mod)
	        change = Moved PlanetSelector class into it's own file
	        change = Removed unnecessary "using" lines
	        change = Updated AssemblyVersion.tt for location-independent builds
            type = fix
        }
        CHANGE
        {
	        change = Added code to check for Kopernicus, and if it does, writes a temporary cfg to disable the on-demand code in Kopernicuus at the next boot to initialize the thumbnails
	        change = Added entry to settings page to force generation of thumbnails at next boot, only available when Kopernicus is installed
            type = add
        }
    }


    VERSION
    {
        version = 0.0.4.1
	
        CHANGE
        {
            change = Fixed a grammer mistake
	        change = Removed KSP_VERSION from the .version file
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.4
	
        CHANGE
        {
            change = Updated code from KittopiaTech to make the thumbnail generation working again
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.3.3
	
        CHANGE
        {
            change = Added KOPERNICUS_NOTES
            type = add
        }
        CHANGE
        {
	        change = Fixed check for pre-existing planet thumbnail, if it exists, don't bother to regenerate
	        change = Build for 1.5
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.3.2
	
        CHANGE
        {
            change = Changed loading of arrows to use the ToolbarControl.LoadImageFromFile rather than relying on the KSP/Unity load
	        change = Deleted Images directory with contents, now using PluginData/Images
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.3.1
	
        CHANGE
        {
            change = Updated up/down arrows
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.3
	
        CHANGE
        {
            change = Added ability to enter orbit period instead of altitude
            type = add
        }
    }

    VERSION
    {
        version = 0.0.2.1
		
        CHANGE
        {
            change = Maneuver nodes are deleted properly,
            type = fix
        }
        CHANGE
        {
		    change = Added an alarm margin to show correct timings in the alarm details
            type = add
        }
    }

    VERSION
    {
        version = 0.0.2
	
        CHANGE
        {
            change = Added configurable SOI color for editor and flight (two colors, white and dark grey)
	        change = Added alternate skin
	        change = Added buttons to get current vessel's Pe or Ap while in flight scene
	        change = Added button to create maneuver node.  All pre-existing maneuver nodes will be removed
            type = add
        }
        CHANGE
        {
	        change = Added Mechjeb integration
		        subchange = Added button to call Mechjeb to execute maneuver node, button will be red if Pe is below the atmosphere limit
            type = add
        }
        CHANGE
        {
	        change = Added button to clear all maneuver nodes
	        change = Added Kerbal Alarm Clock integration
		    change = Added button to add alarms for the orbital maneuver (if Mechjeb not installed) and for each subsequent satellite seperation 
	        change = Added hiding of window when UI is hidden (F2)
	        change = Added optional hiding of window when game is paused
            type = add
        }
    }

    VERSION
    {
        version = 0.0.1.2
	
        CHANGE
        {
            change = Fixed some values being reset between scenes
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.1.1
	
        CHANGE
        {
            change = Fixed update when typing in number of satellites
            type = fix
        }
    }

    VERSION
    {
        version = 0.0.1
        CHANGE
        {
            change = Initial release
            type = add
        }
    }
 }